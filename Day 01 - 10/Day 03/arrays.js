/**
 * ====================================
 * Arrays
 * ====================================
 */

let arr = [23, "Ferrari", true, 53.5];

// accessing the elements
let a = arr[1];
console.log(a);
console.log(arr[3]);

// replacing elements of an array by using index
arr[2] = false;
console.log(arr);

// length of an array
console.log("The length of the array is", arr.length);
