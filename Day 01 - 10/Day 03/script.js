/**
 * ====================================
 * Ternary operator
 * ====================================
 */

// Ternary operator ( ? : )

// let arr = [2, 5, 7, 9];
// arr.forEach((num, index) =>
//   num === 2 ? (arr[index] = "Two") : (arr[index] = "Odd")
// );
// console.log(arr);
